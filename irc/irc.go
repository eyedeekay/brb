package trayirc

import (
	"i2pgit.org/idk/libbrb"
)

func Close(dir, configfile string) {
	trayirc.Close(dir, configfile)
}

func IRCServerMain(version, debug bool, dir, configfile string) {
	IRCServerMain(version, debug, dir, configfile)
}

func OutputAutoLink(dir, configfile string) string {
	return OutputAutoLink(dir, configfile)
}

func IRC(confdir string) {
	IRC(confdir)
}
